---
phase: 01-crawl-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - wwwroot/robots.txt
  - wwwroot/sitemap.xml
  - wwwroot/site.webmanifest
  - Components/App.razor
autonomous: true
requirements:
  - CRAWL-01
  - CRAWL-02
  - CRAWL-03
  - CRAWL-04

must_haves:
  truths:
    - "robots.txt allows Googlebot to crawl the homepage and sitemap.xml"
    - "robots.txt blocks crawling of contract form routes, buyer routes, test pages, and status pages"
    - "sitemap.xml lists only the homepage URL with a valid lastmod date"
    - "The HTML document declares lang=lt (not lang=en)"
    - "Web manifest has populated name, short_name, and description fields"
  artifacts:
    - path: "wwwroot/robots.txt"
      provides: "Crawler access policy"
      contains: "Allow: /"
    - path: "wwwroot/sitemap.xml"
      provides: "Homepage URL with lastmod"
      contains: "<lastmod>"
    - path: "wwwroot/site.webmanifest"
      provides: "Complete web manifest"
      contains: "AutoDokas"
    - path: "Components/App.razor"
      provides: "Lithuanian lang attribute"
      contains: 'lang="lt"'
  key_links:
    - from: "wwwroot/robots.txt"
      to: "wwwroot/sitemap.xml"
      via: "Sitemap directive referencing sitemap.xml URL"
      pattern: "Sitemap:.*sitemap\\.xml"
---

<objective>
Fix the four site-level static files that control how search engines discover and classify autodokas.lt.

Purpose: The current robots.txt blocks all crawling with `Disallow: /`, the sitemap has no lastmod date, the HTML lang attribute is set to "en" instead of "lt", and the web manifest has empty name fields. These are the foundational crawl blockers -- all other SEO work is wasted until these are fixed.

Output: Updated robots.txt, sitemap.xml, site.webmanifest, and App.razor with correct crawl configuration.
</objective>

<execution_context>
@/Users/audriusmasiulionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/audriusmasiulionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crawl-infrastructure/01-CONTEXT.md
@.planning/phases/01-crawl-infrastructure/01-RESEARCH.md

<interfaces>
<!-- No code interfaces needed -- this plan edits static files and one line in App.razor -->
<!-- App.razor line 3 currently reads: <html lang="en"> -->
<!-- All other files are standalone static assets in wwwroot/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rewrite robots.txt, sitemap.xml, and site.webmanifest</name>
  <files>wwwroot/robots.txt, wwwroot/sitemap.xml, wwwroot/site.webmanifest</files>
  <action>
Rewrite wwwroot/robots.txt to allow-by-default with specific Disallow rules per user decision:

```
User-agent: *
Allow: /

Disallow: /contract
Disallow: /buyer/
Disallow: /BuyerNotificationSent
Disallow: /ContractCompleted
Disallow: /email-test
Disallow: /contract-test

Sitemap: https://autodokas.lt/sitemap.xml
```

Key details:
- `Disallow: /contract` (no trailing slash) blocks /contract, /contract/{guid}, and /contract/download/{guid} via prefix matching. This is intentional.
- `Disallow: /buyer/` blocks /buyer/{guid} routes.
- Remove the old `Allow: /$` and `Disallow: /` lines entirely.

Update wwwroot/sitemap.xml to add a lastmod date:

```xml
<?xml version="1.0" encoding="UTF-8"?>
<urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
  <url>
    <loc>https://autodokas.lt/</loc>
    <lastmod>2026-03-03</lastmod>
    <priority>1.0</priority>
  </url>
</urlset>
```

Update wwwroot/site.webmanifest with proper formatting and filled fields per user decision:

```json
{
  "name": "AutoDokas",
  "short_name": "AutoDokas",
  "description": "Automobilio pirkimo pardavimo sutartis internetu",
  "icons": [
    {
      "src": "/android-chrome-192x192.png",
      "sizes": "192x192",
      "type": "image/png"
    },
    {
      "src": "/android-chrome-512x512.png",
      "sizes": "512x512",
      "type": "image/png"
    }
  ],
  "theme_color": "#ffffff",
  "background_color": "#ffffff",
  "display": "standalone"
}
```
  </action>
  <verify>
    <automated>grep -q "Allow: /" wwwroot/robots.txt && grep -q "Disallow: /contract" wwwroot/robots.txt && ! grep -q "Disallow: /$" wwwroot/robots.txt && grep -q "lastmod" wwwroot/sitemap.xml && grep -q '"name": "AutoDokas"' wwwroot/site.webmanifest && grep -q '"description"' wwwroot/site.webmanifest && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>robots.txt allows all crawling by default with specific Disallow rules for app routes, sitemap.xml has lastmod date, site.webmanifest has name/short_name/description filled</done>
</task>

<task type="auto">
  <name>Task 2: Fix HTML lang attribute to Lithuanian</name>
  <files>Components/App.razor</files>
  <action>
In Components/App.razor, change line 3 from:

```razor
<html lang="en">
```

to:

```razor
<html lang="lt">
```

This is a single-character change on one line. Do NOT modify any other content in App.razor.
  </action>
  <verify>
    <automated>grep -q 'lang="lt"' Components/App.razor && ! grep -q 'lang="en"' Components/App.razor && echo "PASS" || echo "FAIL"</automated>
  </verify>
  <done>App.razor declares lang="lt" instead of lang="en", telling search engines and browsers this is a Lithuanian-language site</done>
</task>

</tasks>

<verification>
All four site-level crawl configuration files are correctly updated:
1. `robots.txt` allows general crawling, blocks app routes, references sitemap
2. `sitemap.xml` has the homepage URL with a lastmod date in W3C format
3. `site.webmanifest` has populated name, short_name, and description
4. `App.razor` declares `lang="lt"`
</verification>

<success_criteria>
- robots.txt contains `Allow: /` and `Disallow: /contract` (no longer has `Disallow: /`)
- sitemap.xml contains `<lastmod>2026-03-03</lastmod>`
- site.webmanifest contains `"name": "AutoDokas"` and `"description": "Automobilio pirkimo pardavimo sutartis internetu"`
- App.razor contains `lang="lt"` (not `lang="en"`)
</success_criteria>

<output>
After completion, create `.planning/phases/01-crawl-infrastructure/01-01-SUMMARY.md`
</output>
