---
phase: 01-crawl-infrastructure
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - Components/Pages/Contract/Contract.razor
  - Components/Pages/Contract/BuyerNotificationSent.razor
  - Components/Pages/Contract/ContractCompleted.razor
  - Components/Pages/Contract/ContractDownload.razor
  - Components/Pages/Legal/TermsAndConditions.razor
  - Components/Pages/Legal/PrivacyPolicy.razor
  - Components/Pages/Error.razor
  - Components/TestPages/EmailSendTestPage.razor
  - Components/TestPages/ContractPdfTemplatePage.razor
autonomous: true
requirements:
  - CRAWL-05

must_haves:
  truths:
    - "Contract form pages serve a noindex meta tag in the HTML head"
    - "Legal pages (terms, privacy) serve a noindex meta tag in the HTML head"
    - "Error page serves a noindex meta tag in the HTML head"
    - "Test pages serve a noindex meta tag in the HTML head"
    - "The homepage does NOT have a noindex meta tag"
  artifacts:
    - path: "Components/Pages/Contract/Contract.razor"
      provides: "noindex for /contract, /contract/{guid}, /buyer/{guid}"
      contains: '<meta name="robots" content="noindex"'
    - path: "Components/Pages/Contract/BuyerNotificationSent.razor"
      provides: "noindex for /BuyerNotificationSent"
      contains: '<meta name="robots" content="noindex"'
    - path: "Components/Pages/Contract/ContractCompleted.razor"
      provides: "noindex for /ContractCompleted"
      contains: '<meta name="robots" content="noindex"'
    - path: "Components/Pages/Contract/ContractDownload.razor"
      provides: "noindex for /contract/download/{guid}"
      contains: '<meta name="robots" content="noindex"'
    - path: "Components/Pages/Legal/TermsAndConditions.razor"
      provides: "noindex for /terms-and-conditions"
      contains: '<meta name="robots" content="noindex"'
    - path: "Components/Pages/Legal/PrivacyPolicy.razor"
      provides: "noindex for /privacy-policy"
      contains: '<meta name="robots" content="noindex"'
    - path: "Components/Pages/Error.razor"
      provides: "noindex for /Error"
      contains: '<meta name="robots" content="noindex"'
    - path: "Components/TestPages/EmailSendTestPage.razor"
      provides: "noindex for /email-test"
      contains: '<meta name="robots" content="noindex"'
    - path: "Components/TestPages/ContractPdfTemplatePage.razor"
      provides: "noindex for /contract-test"
      contains: '<meta name="robots" content="noindex"'
  key_links:
    - from: "Components/Pages/*/**.razor (noindex pages)"
      to: "Components/App.razor HeadOutlet"
      via: "HeadContent component renders into HeadOutlet in App.razor <head>"
      pattern: "<HeadContent>.*noindex.*</HeadContent>"
---

<objective>
Add noindex meta tags to all 9 non-content pages so search engines do not index contract forms, legal pages, error pages, or test pages.

Purpose: Only the homepage should appear in Google search results. These pages have no SEO value and indexing them would dilute site quality signals. The noindex meta tag is the primary mechanism for legal pages (not blocked by robots.txt) and a defense-in-depth layer for app routes (also blocked by robots.txt in Plan 01).

Output: 9 .razor files updated with `<HeadContent><meta name="robots" content="noindex" /></HeadContent>` blocks.
</objective>

<execution_context>
@/Users/audriusmasiulionis/.claude/get-shit-done/workflows/execute-plan.md
@/Users/audriusmasiulionis/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-crawl-infrastructure/01-CONTEXT.md
@.planning/phases/01-crawl-infrastructure/01-RESEARCH.md

<interfaces>
<!-- HeadContent is a built-in Blazor component from Microsoft.AspNetCore.Components.Web -->
<!-- Already imported globally via _Imports.razor -- no additional @using needed -->
<!-- HeadOutlet in App.razor (line 36) collects all HeadContent output into the HTML <head> -->

<!-- Pattern for pages WITH existing PageTitle: -->
<!-- Insert <HeadContent> block AFTER the existing <PageTitle> line -->

<!-- Pattern for pages WITHOUT existing PageTitle: -->
<!-- Insert <HeadContent> block AFTER the last @-directive line, before the first HTML element -->

Current file structures (first lines):

Contract.razor:
  @page "/contract"
  @page "/contract/{ContractId:guid}"
  @page "/buyer/{ContractId:guid}"
  @attribute [StreamRendering]
  @rendermode InteractiveServer
  ... (4 @using lines) ...
  <PageTitle>...</PageTitle>
  --> INSERT <HeadContent> AFTER <PageTitle>

BuyerNotificationSent.razor:
  @page "/BuyerNotificationSent"
  @attribute [StreamRendering]
  @rendermode InteractiveServer
  --> NO PageTitle exists. INSERT <HeadContent> AFTER @rendermode line, before <div>

ContractCompleted.razor:
  @page "/ContractCompleted"
  @attribute [StreamRendering]
  @rendermode InteractiveServer
  --> NO PageTitle exists. INSERT <HeadContent> AFTER @rendermode line, before <div>

ContractDownload.razor:
  @page "/contract/download/{ContractId:guid}"
  @attribute [StreamRendering]
  @rendermode InteractiveServer
  ... (@using, @inherits lines) ...
  <PageTitle>Sutarties atsisiuntimas</PageTitle>
  --> INSERT <HeadContent> AFTER <PageTitle>

TermsAndConditions.razor:
  @page "/terms-and-conditions"
  @using AutoDokas.Resources
  <PageTitle>@Text.TermsAndConditions</PageTitle>
  --> INSERT <HeadContent> AFTER <PageTitle>

PrivacyPolicy.razor:
  @page "/privacy-policy"
  @using AutoDokas.Resources
  <PageTitle>@Text.PrivacyPolicy</PageTitle>
  --> INSERT <HeadContent> AFTER <PageTitle>

Error.razor:
  @page "/Error"
  <PageTitle>Klaida</PageTitle>
  --> INSERT <HeadContent> AFTER <PageTitle>

EmailSendTestPage.razor:
  @page "/email-test"
  @rendermode InteractiveServer
  ... (@using, @inject, @layout lines) ...
  @if (!Env.IsDevelopment()) { return; }
  --> INSERT <HeadContent> AFTER @if line, before <div>

ContractPdfTemplatePage.razor:
  @page "/contract-test"
  ... (@using, @inject, @layout lines) ...
  @if (!Env.IsDevelopment()) { return; }
  --> INSERT <HeadContent> AFTER @if line, before content
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add noindex to contract and status pages (4 files)</name>
  <files>Components/Pages/Contract/Contract.razor, Components/Pages/Contract/BuyerNotificationSent.razor, Components/Pages/Contract/ContractCompleted.razor, Components/Pages/Contract/ContractDownload.razor</files>
  <action>
Add a `<HeadContent>` block with a noindex meta tag to each of the 4 contract-related pages. Use inline HeadContent per page (not a shared component -- per user's Claude's Discretion allowance and research recommendation).

For each file, add the following block:
```razor
<HeadContent>
    <meta name="robots" content="noindex" />
</HeadContent>
```

Placement rules:
- **Contract.razor**: Insert AFTER the existing `<PageTitle>` line (after line 11). Do NOT change any other content.
- **BuyerNotificationSent.razor**: Insert AFTER `@rendermode InteractiveServer` (after line 3), with a blank line before the existing `<div>`. This page has no `<PageTitle>`.
- **ContractCompleted.razor**: Insert AFTER `@rendermode InteractiveServer` (after line 3), with a blank line before the existing `<div>`. This page has no `<PageTitle>`.
- **ContractDownload.razor**: Insert AFTER the existing `<PageTitle>` line (after line 9). Do NOT change any other content.

The `<HeadContent>` component is already available (imported via _Imports.razor). No additional @using statements needed. The noindex meta tag is a hardcoded string with no async dependency, so it renders correctly during both static SSR prerendering and InteractiveServer prerendering.
  </action>
  <verify>
    <automated>for f in Components/Pages/Contract/Contract.razor Components/Pages/Contract/BuyerNotificationSent.razor Components/Pages/Contract/ContractCompleted.razor Components/Pages/Contract/ContractDownload.razor; do grep -q 'name="robots" content="noindex"' "$f" || { echo "FAIL: $f missing noindex"; exit 1; }; done && echo "PASS"</automated>
  </verify>
  <done>All 4 contract/status pages contain a noindex meta tag inside HeadContent: Contract.razor (covers /contract, /contract/{guid}, /buyer/{guid}), BuyerNotificationSent.razor, ContractCompleted.razor, ContractDownload.razor</done>
</task>

<task type="auto">
  <name>Task 2: Add noindex to legal, error, and test pages (5 files)</name>
  <files>Components/Pages/Legal/TermsAndConditions.razor, Components/Pages/Legal/PrivacyPolicy.razor, Components/Pages/Error.razor, Components/TestPages/EmailSendTestPage.razor, Components/TestPages/ContractPdfTemplatePage.razor</files>
  <action>
Add a `<HeadContent>` block with a noindex meta tag to each of the 5 remaining pages:

```razor
<HeadContent>
    <meta name="robots" content="noindex" />
</HeadContent>
```

Placement rules:
- **TermsAndConditions.razor**: Insert AFTER `<PageTitle>@Text.TermsAndConditions</PageTitle>` (after line 4), before the blank line and `<div>`.
- **PrivacyPolicy.razor**: Insert AFTER `<PageTitle>@Text.PrivacyPolicy</PageTitle>` (after line 4), before the blank line and `<div>`.
- **Error.razor**: Insert AFTER `<PageTitle>Klaida</PageTitle>` (after line 3), before the blank line and `<div>`.
- **EmailSendTestPage.razor**: Insert AFTER the `@if (!Env.IsDevelopment()) { return; }` line (after line 8), before the `<div>`. Note: even though this page has a dev-only guard, the noindex tag should still be present as defense-in-depth.
- **ContractPdfTemplatePage.razor**: Insert AFTER the `@if (!Env.IsDevelopment()) { return; }` line (after line 7), before the content. Same defense-in-depth rationale.

No additional @using statements needed. Do NOT modify any existing content in these files.

IMPORTANT: Do NOT add noindex to Components/Pages/Home.razor -- the homepage must remain indexable.
  </action>
  <verify>
    <automated>for f in Components/Pages/Legal/TermsAndConditions.razor Components/Pages/Legal/PrivacyPolicy.razor Components/Pages/Error.razor Components/TestPages/EmailSendTestPage.razor Components/TestPages/ContractPdfTemplatePage.razor; do grep -q 'name="robots" content="noindex"' "$f" || { echo "FAIL: $f missing noindex"; exit 1; }; done && ! grep -q 'noindex' Components/Pages/Home.razor && echo "PASS" || echo "FAIL: Home.razor should NOT have noindex"</automated>
  </verify>
  <done>All 5 legal/error/test pages contain a noindex meta tag inside HeadContent. Home.razor does NOT have a noindex tag. Total: 9 of 9 non-content pages now have noindex.</done>
</task>

</tasks>

<verification>
All 9 non-content pages have noindex meta tags. The homepage does NOT:
1. Contract.razor -- covers /contract, /contract/{guid}, /buyer/{guid}
2. BuyerNotificationSent.razor -- covers /BuyerNotificationSent
3. ContractCompleted.razor -- covers /ContractCompleted
4. ContractDownload.razor -- covers /contract/download/{guid}
5. TermsAndConditions.razor -- covers /terms-and-conditions
6. PrivacyPolicy.razor -- covers /privacy-policy
7. Error.razor -- covers /Error
8. EmailSendTestPage.razor -- covers /email-test
9. ContractPdfTemplatePage.razor -- covers /contract-test

Verify with: `grep -rn 'noindex' Components/Pages/ Components/TestPages/ | grep -v '.razor.cs'`
Expected: 9 matches (one per file above), none in Home.razor.
</verification>

<success_criteria>
- All 9 .razor pages listed above contain `<meta name="robots" content="noindex" />` inside a `<HeadContent>` block
- Components/Pages/Home.razor does NOT contain "noindex"
- No other files are modified
</success_criteria>

<output>
After completion, create `.planning/phases/01-crawl-infrastructure/01-02-SUMMARY.md`
</output>
